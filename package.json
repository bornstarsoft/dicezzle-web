{
  "name": "dicezzle-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build:game && hugo server --bind 127.0.0.1 --baseURL http://127.0.0.1:1313/",
    "build": "vite build && hugo --gc --minify",
    "build:game": "vite build",
    "test": "vitest run"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "vite": "^7.0.0",
    "vitest": "^3.2.0"
  }
}
